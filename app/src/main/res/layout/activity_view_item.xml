<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" android:layout_width="match_parent"
    android:layout_height="match_parent" android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin"
    tools:context="edu.hmc.dxie.bucketlist.ViewItemActivity">

    <!-- Empty layout for aligning widgets to horizontal center -->
    <Space android:id="@+id/center_dummy"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:layout_centerHorizontal="true"
        android:visibility="invisible"/>
    
    <!-- Text of item being viewed -->
    <TextView 
        android:id="@+id/text_itemtext"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="150dp"
        android:layout_centerHorizontal="true"
        android:textSize="24sp" />

    <!-- Complete button -->
    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Complete"
        android:id="@+id/button_complete"
        android:layout_below="@+id/text_itemtext"
        android:layout_alignRight="@+id/center_dummy"
        android:clickable="true" />
    
    <!-- Delete button -->
    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Delete"
        android:id="@+id/button_delete"
        android:layout_below="@+id/text_itemtext"
        android:layout_alignLeft="@+id/center_dummy"
        android:clickable="true" />

</RelativeLayout>
